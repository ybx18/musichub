# Security Policy

## 安全模型

MusicHub 是一个**纯前端、零服务器**的开源框架：

- 不部署任何自有后端，不持有任何平台的账号或 Cookie。
- 不收集、不上报任何用户数据；所有本地数据仅存于浏览器 `localStorage`。
- 所有音频请求均由使用者自行配置的音源接口处理，请求直接从浏览器发往第三方。

## 报告漏洞

**请勿在公开 GitHub Issue 中披露安全漏洞。**

请通过以下方式私下报告：

- 在本仓库发起一个 **private security advisory**（GitHub → Security → Advisories → Report a vulnerability）；
- 或在提交 PR/Issue 时仅描述「存在安全隐患，请私下联系维护者」，由维护者主动联系你。

收到报告后，维护者会：

1. 确认问题并评估影响；
2. 在私下沟通中协商修复与披露时间线；
3. 修复完成后公开致谢（除非你要求匿名）。

## 适用范围

以下情况属于安全范畴：

- 静态服务器（`tools/serve.mjs` / `serve.py`）的目录穿越、请求劫持等；
- 可能导致用户 Cookie / 本地数据泄漏的逻辑；
- 依赖供应链风险（本仓库零运行时依赖，但仍欢迎审视）。

## 注意

本仓库**默认不内置任何音源**。若你自行配置的音源接口存在安全或合规风险，由配置者自行承担。
